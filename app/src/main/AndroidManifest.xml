<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/achtoicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/achtoicon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Inventario_achto"
        tools:targetApi="31">

        <activity
            android:name=".ActividadesPantallas.inicio_sesion"
            android:exported="true"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RECIBO.pantalla_recibo"/>
        <activity android:name=".ActividadesPantallas.inicio_menu"/>
        <activity android:name=".EMBARQUE.cantidad_embarque"/>
        <activity android:name=".CONSULTA.pantalla_consulta"/>

    </application>
</manifest>